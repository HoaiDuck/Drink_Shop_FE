import React, { useState, useEffect, useContext } from "react";

const VisitedRequest = ({ id }) => {
  return (
    <div>
      <h1 className="font-bold">Open request terms</h1>
    </div>
  );
};

export default VisitedRequest;
